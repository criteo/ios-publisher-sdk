# This file contains the fastlane.tools configuration
# You can find the documentation at https://docs.fastlane.tools
#
# For a list of all available actions, check out
#
#     https://docs.fastlane.tools/actions
#
# For a list of all available plugins, check out
#
#     https://docs.fastlane.tools/plugins/available-plugins
#

default_platform(:ios)

platform :ios do
  desc "Run Fuji Tests"
  lane :fuji_tests do |options|
    reset_simulator_contents() # Not needed on CI with virtualized env
    tests_result = multi_scan(scheme: options[:scheme], try_count: 3, fail_build: false)
    reports = Dir['test_output/*/*.html'].map { |relpath| File.absolute_path(relpath) }
    collate_html_reports(reports: reports, collated_report: File.join('build/reports', 'tests.html'))
    reports = Dir['test_output/*/*.junit'].map { |relpath| File.absolute_path(relpath) }
    collate_junit_reports(reports: reports, collated_report: File.join('build/reports', 'junit.xml'))
    failed_tests = tests_result[:failed_tests]
    unless failed_tests.size.zero?
      UI.error("Tests failure")
    end
  end

  desc "Run Pre Submit Tests"
  lane :presubmit_tests do
    fuji_tests(scheme: "PresubmitTests")
  end
  desc "Run Post Submit Tests"
  lane :postsubmit_tests do
    fuji_tests(scheme: "PostsubmitTests")
  end
end
